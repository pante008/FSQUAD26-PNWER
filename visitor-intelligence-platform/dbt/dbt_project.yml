name: 'visitor_analytics'
version: '1.0.0'
config-version: 2

profile: 'visitor_analytics'

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
data-paths: ["data"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  visitor_analytics:
    staging:
      materialized: view
      schema: STAGING
    intermediate:
      materialized: table
      schema: INTERMEDIATE
    marts:
      materialized: table
      schema: ANALYTICS